# Input Schemas

GridKit input formats are defined by JSON Schemas generated at
documentation build time and served by Read the Docs at stable URLs for
editor and CI validation. The repository commits the generator, not the
JSON artifact; each schema page describes the format and renders the
generated schema.

```{toctree}
:maxdepth: 2
:titlesonly:

Overhead Line <EMT/line>
```
