# Input Schemas

GridKit input formats are defined by authored JSON Schemas committed with the
model code. Each schema page renders the authoritative schema and links the raw
file for editor and CI validation.

```{toctree}
:maxdepth: 2
:titlesonly:

Overhead Line <EMT/line>
```
