# Signal Bus

A signal bus is allows multiple models to read/access information.


## Application

Through a signal bus object, models are capable of reading/writing to 
a shared complex signal (either voltage-type or current-type).

Real-valued signals are represented through $V_r$, the real part of the signal bus voltage.
